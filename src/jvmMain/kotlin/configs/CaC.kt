package configs

import androidx.compose.ui.unit.DpSize
import androidx.compose.ui.unit.dp

val globalAppWindowSize = DpSize(width = 300.dp, height = 330.dp)

val globalGameType = GameType.SNAKE
const val globalFieldSize = 20
const val globalDefaultTactDuration = 1000L